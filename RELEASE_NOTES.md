## v1.3.0

- 新增 Android「后台保活」（默认开启，悬浮球-更多-偏好里可关）：应用退到后台时挂一条前台服务常驻通知，进程和网络不受系统后台限制，ZCode 会话的 WebSocket 不断开——回到应用不再整页刷新重连。iOS 系统不允许第三方后台保活，无法实现同类功能；桌面端不受影响

## v1.2.9

- **macOS 内核更换：网页承载从内嵌 WKWebView 换成 CEF（Chromium）**，根治中文输入法问题——此前 Flutter 平台视图层会破坏 WKWebView 的输入法组合态（Safari/Chrome 正常、仅本应用坏），CEF 自带原生输入法管线且不经过平台视图的键盘路由。macOS 包体积从 13MB 增至约 120MB（Chromium 内核固有成本），且仅支持 Apple Silicon（arm64）与 macOS 12.0+（CEF 上游暂无 Intel/universal 构建）；Android/iOS/Windows 完全不受影响（Android 系统 WebView、Windows WebView2 本就是 Chromium）
- macOS 已知取舍：JS 弹窗/右键菜单为 Chromium 默认行为；页面加载进度条改为流动动画（CEF 无进度事件）；「桌面版网站」开关在 macOS 隐藏（CEF 本就是桌面 Chromium）
- 其他平台的缩放/平移/双指开关行为不变

## v1.2.8

- 下载进度条重做三态显示：连接服务器期间显示「正在连接…」（此前显示假的 0.0/0.0 MB 卡着不动）；字节下载完到文件拼好之间显示「下载完成，正在校验与拼装…」加满格进度条（此前切成不定态来回扫的动画，看起来像进度条又跑了一遍）
- 分段下载因远端校验不一致从头重下时，现在会提示「下载源校验不一致，正在从头重新下载…」，进度条归零不再莫名其妙（此前第一遍走完又从头走一遍没有任何说明）
- 拿不到文件总大小时如实显示「大小未知」（弹窗与通知栏），不再出现瞬间走满的假百分比

## v1.2.7

- macOS「双指缩放」按钮置灰不可点：WKWebView 只有整页缩放、没有系统级字体缩放，触摸板捏合做出来也是整页缩放，按要求不做（v1.2.6 的触摸板捏合实现已撤下）
- 修复 Android 双指缩放「默认关」不生效：插件默认是开，此前创建 WebView 时未按开关状态显式关闭，现在创建时即应用开关状态

## v1.2.6

- 输入防护全部撤下：移除自 v1.1.3 加入的输入法组合态回车防护及后续所有发送后补救，回到 v1.0.x 的裸 WebView 状态重新分析输入问题（副作用：macOS 上组词途中按回车可能再次被页面误执行命令，属预期回归，遇到请反馈）
- 新增「双指缩放」开关（工具栏，Android 双指 / macOS 触摸板捏合），默认关闭、记住选择；macOS 开启后触摸板捏合直接调节页面缩放，与 +/- 按钮同一份数值
- 「回到 100%」重新合并为一个：同时复位页面缩放与 Android 双指缩放（撤回 v1.2.5 拆成两个 100% 的做法）

## v1.2.5

- macOS 输入回归极简：移除 v1.2.4 的发送后焦点循环，注入脚本回到只做组合态回车防护的最初形态，不再对输入做任何额外处理；页面加载后也不再重发缩放设置（原生缩放本来就跨加载保留）。输入异常时应急：点一下输入框外再点回，或用工具栏「在系统浏览器打开」
- Android 拆成两个独立的 100%：平移开关下方新增「双指缩放回到 100%」按钮（复位手势缩放），+/- 组中间的百分比只复位字体缩放，互不干扰

## v1.2.4

- macOS 中文输入法重修：上一版的补救只做了 DOM 级失焦/聚焦，重建不了输入法会话；现在改为检测到消息发送完成后，在原生层做一次焦点循环（等价"点输入框外再点回"），强制 WKWebView 重建失效的文本输入会话
- Android「页面缩放」改为字体缩放（系统级 textZoom）：只放大字体并按新字号重排，不再是 v1.2.3 那种与双指缩放同源的整幅画面放大；双指缩放/平移保持不变，「回到 100%」同时复位字体缩放和双指缩放

## v1.2.3

- 回退 v1.1.12 的 macOS「发送后自动重聚焦」：实测让中文输入法从第一条消息起就打不进（英文直输不受影响），越修越糟，整体回退。macOS 内嵌页中文输入异常时：点一下输入框外再点回可强制重建输入；悬浮工具栏新增「在系统浏览器打开」一等按钮（仅 macOS），输入出问题就交给系统浏览器，不再堆注入补丁
- 缩放重构：移除「可视缩放」，恢复「页面缩放」并统一为浏览器 Ctrl +/- 语义——macOS/iOS 用 WKWebView 原生 pageZoom，Windows 用根节点 CSS 缩放，Android 改为直接驱动 WebView 原生缩放（与双指缩放同一份，不会再把 fixed 输入框挤出屏幕）
- Android 保留原生双指缩放/平移；「回到 100%」（点百分比）同时复位按钮缩放和双指缩放，展开工具栏时百分比会与双指实际倍率同步

## v1.2.2

- 版本编号规则变更（本版与 v1.1.12 无功能差异）：此后每次发版末位 +1，某一位超过 9 即向前进位（1.2.9 → 1.3.0，1.9.9 → 2.0.0）；原 1.1.12 按新规则归一为本版号

## v1.1.12

- 修复 macOS 上发送一条消息后无法再输入的问题（打一个字像被删一个字）：页面发送后会清空仍持有焦点的输入框，macOS WKWebView 的文本输入会话残留失效；现检测到发送完成后自动重建输入会话（对输入框做一次不可见的失焦/聚焦）

## v1.1.11

- 移除「页面缩放」：Android 的 WebView 已原生支持双指缩放和平移，应用内重排缩放失去意义，且在 Android 上把 ZCode 输入框挤出屏幕外（输入框挂在 fixed/视口单位上，不跟随 body 缩放）
- 「回到 100%」（点百分比数字）现在会把 Android 原生双指缩放一并复位；切换会话时的缩放重置同样包含原生缩放
- 平移（拖动）模式不再被 WebView 自身滚动干扰：开启期间停用下拉刷新（顶部下拉不再触发刷新抢手势），拖拽层铺满整个可平移区域（原生双指缩放后一屏之外也能拖）

## v1.1.10

- 修复 Android 上「页面缩放」完全无效的问题：Android WebView 对根元素（<html>）的 CSS 缩放不生效，现改为作用于 <body>，与「可视缩放」各自独立、可叠加
