## v1.5.2

- **收窄回车拦截窗口，避免干扰编辑器的输入法组合状态机**：查证 Lexical 源码与已知问题（#8179/#8697/#6940）发现，编辑器靠 keydown 给组合输入「收尾」（WebKit 的 ending 相位），组合刚结束就止住回车传播会让状态机收不了尾，回同步时误删当前行文本。现拦截只作用于「组合结束 100ms 之外」的明确回车；组合期间与刚结束的回车完全不碰，交给编辑器自己的管线（页面的误发送仍由回车防护脚本兜住）

## v1.5.1

- **修复输入法「整理文本」、语音转写等非按键换行仍吞文本的问题（v1.5.0）**：这类换行不走键盘事件、直接产生 beforeinput，v1.5.0 里移动端兜底的 beforeinput 拦截会把它止住传播，而编辑器恰恰靠监听 beforeinput 同步内部状态——收不到就与 DOM 失同步，React 重新协调时把整行文本吞掉。根修：彻底删除 beforeinput 拦截，本脚本唯一的干预点收敛为「干净回车」的 keydown，其余一切输入事件（含输入法的所有文本/换行操作）全部放行，交给编辑器自己的管线

## v1.5.0

- **修复换行后整行文本消失的问题（v1.4.9）**：上一版用 execCommand 手动插入换行，与 Lexical/React 的状态同步冲突，换行时当前行文本会被吞掉（包括输入法整理文本触发的换行）。新方案彻底不碰 execCommand、不 preventDefault、不手动插内容：回车键在输入框里的原生默认行为本来就是换行，页面之所以发送是它自己把默认行为拦了——我们只在 window 捕获阶段止住事件传播（页面收不到、拦不了默认行为），浏览器原生的换行自然发生，编辑器状态零风险

## v1.4.9

- **修复回车换行拦截不住的问题（v1.4.8 及更早）**：此前拦截挂在 document 捕获阶段，而页面若把自己的回车监听挂在 window 捕获阶段，会在我们之前执行、直接把消息发出去。现改挂 window 捕获（注入早于页面脚本，先注册先执行）并用 stopImmediatePropagation 压住后注册的同层监听
- **Android/iOS 软键盘回车也改为换行**：软键盘回车常不产生可拦截的 keydown，补了 beforeinput 层的 insertParagraph 拦截。安全兜底：只有页面上找得到发送按钮时才拦截换行，找不到则保持原生「回车即发送」，保证消息永远发得出去（桌面端不受此限，可用 Cmd/Ctrl+Enter 发送）

## v1.4.8

- **Android 视图复位按钮改义**：平移开关下方的复位按钮现在是「视图复位」——双指缩放和字体缩放一起归位，页面回到刚打开时的整页适配状态（此前只归双指倍率，字体缩放残留时排版依然不是初始适配的样子）
- **切换 tab 保留各会话的缩放**：切到别的会话再切回来，缩放还是你上次调的（每个会话独立记住）；应用重启后不保留，回到默认 1.0（同时移除了此前「缩放持久化、重启恢复」的行为）

## v1.4.7

- **桌面端输入框回车重映射：Enter 插入换行，Cmd/Ctrl+Enter 发送**（macOS 用 Cmd+Enter，Windows 用 Ctrl+Enter）。裸回车不再直接把消息发出去，打多行内容不用再怕误触；发送优先点击页面的发送按钮，页面没有独立按钮时向输入框重放回车交给页面自身逻辑。输入法不受影响：拼音候选的提交回车（组合态）仍走输入法原生行为。手机端保持 Enter 直接发送不变（软键盘没有组合键）

## v1.4.6

- **新增顶部会话页签栏（浏览器风格 tab 条）**：玻璃材质横条钉在页面顶部，每个打开的会话一个页签，点击即切换（多会话保活不重连），当前会话高亮；页签带关闭按钮，最右「+」打开会话切换面板（选设备/新建/管理统一入口）。会话多时横向滚动；页签标题用设备备注，没有备注时用地址主机名（连接地址含凭证，不会整串上屏）。Android 上页签栏竖向占位不遮网页内容，iOS 上悬浮于安全区顶部

## v1.4.5

- **重做 Android 安全区方案：底部内容被挤出屏幕、顶部露白两个问题一起解决**。上一版给网页注入安全区 padding 的做法与页面自身 100vh 满高布局冲突（底部溢出），画布刷色也覆盖不到所有页面结构（顶部仍白）。现改回原生 SafeArea 裁出安全区（页面视口即可视区，满高布局天然正确），并从网页实时读取页面实际背景色（加载完成 + 800ms 后各取一次，body 透明时向下找第一个带背景的容器），把露出的区域刷成与页面同色——黑色页面配黑色保留区，不再露白
- **状态栏/手势条图标亮暗跟页面背景色走**：近黑页面自动用浅色图标（此前跟系统主题，深色页面配深色图标看不清）；无会话时仍跟系统主题

## v1.4.4

- **安全区露出区域与页面同色**：内容缩进后顶部/底部露出的是画布（html）背景，默认白色，与页面本体颜色不一致显得突兀。现加载后自动读取页面实际背景色（body 透明时向下找第一个带背景的容器）刷到画布上；暗色模式切换（html class 变化）和 SPA 内容后挂载（body 子树变化，去抖 300ms）都会自动重刷

## v1.4.3

- **补上 v1.4.2 安全区方案的一个布局缺陷**：应用型网页（zcode 即是）几乎都是 `html/body height:100%` 的满高布局，CSS 默认 content-box 下注入的 `body padding` 会让实际高度变成 100% + 安全区，底部输入条反而被推出屏幕外。现给 html/body 补 `box-sizing: border-box`，padding 计入 100% 之内，内容区正确收缩

