## v1.6.4

- **修复 Android 下载更新切后台断连（续 v1.6.3）**：v1.6.3 在切后台的生命周期回调里补挂前台服务，但该回调（onStop）触发时应用已被系统判定为后台，Android 12+ 会直接拒绝启动前台服务且异常被静默吞掉——服务实际没挂上，下载照样断。现改为**点「立即更新」开始下载的那一刻就挂上前台服务**（此时应用必在前台，启动必定成功），整段下载全程受保护；inactive（onPause）时若仍在下载再补挂一次兜底。下载结束（完成/取消/失败）后，没开「后台保活」则自动撤掉服务

## v1.6.3

- **修复 Android 下载更新时切换应用导致下载连接断开的问题**：之前切后台是否挂前台服务跟随「后台保活」开关，开关关着时切后台系统立即限制后台联网，更新下载就断了。现在**更新包正在下载时切后台一定挂前台服务**（与保活开关无关），下载连接在后台不受限；下载在后台结束后若没开保活则自动撤掉服务不留常驻通知，服务通知文案也按用途显示（后台下载 / 保持会话连接）

## v1.6.2

- **修复手机上 tab 栏划不动、没法滚动选页签的问题**：页签之前是「按下即拖」，把 tab 栏的横向滚动手势抢走了——手机上想左右划着找页签，会直接把页签拖起来。现手机上改为**长按页签才进入拖动**（拖动目标：另一个分屏窗格），直接划动就是滚动列表；桌面端鼠标拖和滚轮滚动不冲突，保持按下即可拖

## v1.6.1

- **分屏后每个窗格各有一个小号悬浮球**：之前分屏时悬浮球整个隐藏，导航/缩放/平移都用不了。现在左右窗格各挂一个小号悬浮球，点击展开工具栏，只控制自己窗格的会话：后退/前进/刷新、平移视野、视图复位、页面缩放、桌面版网站、替换地址、设置等全部可用。小球可在本窗格内自由拖动，闲置几秒自动贴到本窗格的外侧边缘并淡化；它的位置不写入全局，不影响单屏悬浮球记住的位置

## v1.6.0

- **分屏改版：每个窗格各带一条页签栏**。开启分屏后顶部全局页签栏收起，左右窗格顶部各自有一条页签栏——点哪个窗格里的页签，就切换哪个窗格显示的页面；窗格的「刷新」「+」「关闭」按钮也各管各的窗格
- **拖动页签换窗格**：把页签从一个窗格拖到另一个窗格松手即挪过去；拖/点对方窗格正在显示的页签则两侧互换。右窗格的会话被关闭时自动由其他会话顶上、不再直接退出分屏；点任一窗格页签栏上的分屏按钮退出分屏

## v1.5.9

- **分屏状态支持拖动页签指定右侧页面**：分屏开启后，按住顶部某个页签拖到右侧窗格松手，该会话即成为右侧面板（拖入时右侧面板底色会淡高亮提示）；拖到其他地方松手则不生效。左侧仍是当前会话不变

## v1.5.8

- **顶部 tab 栏新增两个按钮**：「刷新」重载当前会话；「分屏」开关左右分屏（至少两个会话时才可用）
- **左右分屏：两个页面同时显示**。开启后当前会话在左、另一会话在右，各占一半；中间分隔条按住拖动可调整两边比例（15%~85%）。两个都是保活的现有会话，切进去不重连、各自缩放保留。分屏时悬浮球暂时隐藏（不碍分隔条），退出分屏即恢复

## v1.5.7

- **修复 macOS / Windows 上顶部 tab 栏与网页顶栏重叠**：桌面端网页普遍把顶栏固定在视口最顶端，悬浮叠放的 tab 条会压住内容（macOS 上盖住 zcode 的顶部搜索/会话栏）。现 macOS / Windows 的 tab 栏改为竖向占位（网页从 tab 栏下方开始，不再重叠）；iOS 保持悬浮在安全区顶部不动（不压内容）

## v1.5.6

- **回滚输入法组合文本的自动回填（v1.5.3~v1.5.5）**：实测回填在「整理文本」的多轮重排场景下弊大于利（误判导致重复插入、行序错乱），且该问题的根源是页面所用 Lexical 编辑器自身的已知 bug（facebook/lexical #8179，Safari 原生同样复现），宿主侧无法可靠补救。本版恢复到 v1.5.2 的行为：不做任何回填，回车/换行相关的防护与重映射保持不变。根治需 zcode 页面升级 Lexical

## v1.5.5

- **回填判定等待文本稳定**：「整理文本」是反复多轮重排的过程，固定 100ms 判定太早、可能打断整理。现改为：组合结束后每 150ms 轮询（上限约 2.5 秒），编辑器内容还在变就继续等，连续两拍不变后再做「是否整段被吞」的判定；期间新一轮组合开始则放弃本次判定交给下一轮

