## v1.5.8

- **顶部 tab 栏新增两个按钮**：「刷新」重载当前会话；「分屏」开关左右分屏（至少两个会话时才可用）
- **左右分屏：两个页面同时显示**。开启后当前会话在左、另一会话在右，各占一半；中间分隔条按住拖动可调整两边比例（15%~85%）。两个都是保活的现有会话，切进去不重连、各自缩放保留。分屏时悬浮球暂时隐藏（不碍分隔条），退出分屏即恢复

## v1.5.7

- **修复 macOS / Windows 上顶部 tab 栏与网页顶栏重叠**：桌面端网页普遍把顶栏固定在视口最顶端，悬浮叠放的 tab 条会压住内容（macOS 上盖住 zcode 的顶部搜索/会话栏）。现 macOS / Windows 的 tab 栏改为竖向占位（网页从 tab 栏下方开始，不再重叠）；iOS 保持悬浮在安全区顶部不动（不压内容）

## v1.5.6

- **回滚输入法组合文本的自动回填（v1.5.3~v1.5.5）**：实测回填在「整理文本」的多轮重排场景下弊大于利（误判导致重复插入、行序错乱），且该问题的根源是页面所用 Lexical 编辑器自身的已知 bug（facebook/lexical #8179，Safari 原生同样复现），宿主侧无法可靠补救。本版恢复到 v1.5.2 的行为：不做任何回填，回车/换行相关的防护与重映射保持不变。根治需 zcode 页面升级 Lexical

## v1.5.5

- **回填判定等待文本稳定**：「整理文本」是反复多轮重排的过程，固定 100ms 判定太早、可能打断整理。现改为：组合结束后每 150ms 轮询（上限约 2.5 秒），编辑器内容还在变就继续等，连续两拍不变后再做「是否整段被吞」的判定；期间新一轮组合开始则放弃本次判定交给下一轮

## v1.5.4

- **修复回填导致的行序错乱（v1.5.3）**：回填判定只查「组合文本是否原样出现在编辑器里」，而「整理文本」的最终内容是被页面重排过的多行文本，原样匹配不上就误判为被吞、整段又插了一次，和已插入内容叠出乱序。现改为精确判定：只在组合结束后编辑器内容与组合开始前**一模一样**（整段被吞，一个字都没进去）时才回填；编辑器有任何插入（哪怕顺序不对）都不碰

## v1.5.3

- **输入法组合文本被吞后自动补回**：定案——换行/整理时当前行文本消失是页面所用 Lexical 编辑器自身的已知 bug（facebook/lexical #8179，组合提交时在检查数据前就拦截默认行为把文本清掉；iPhone Safari 原生打开同样复现），页面层治不了本，本版做补救：跟踪每次输入法组合的最新文本，组合结束 100ms 后若发现这段文本没出现在编辑器里，自动用常规打字路径原地补回（该路径编辑器状态处理安全）。根治需要 zcode 页面升级 Lexical

## v1.5.2

- **收窄回车拦截窗口，避免干扰编辑器的输入法组合状态机**：查证 Lexical 源码与已知问题（#8179/#8697/#6940）发现，编辑器靠 keydown 给组合输入「收尾」（WebKit 的 ending 相位），组合刚结束就止住回车传播会让状态机收不了尾，回同步时误删当前行文本。现拦截只作用于「组合结束 100ms 之外」的明确回车；组合期间与刚结束的回车完全不碰，交给编辑器自己的管线（页面的误发送仍由回车防护脚本兜住）

## v1.5.1

- **修复输入法「整理文本」、语音转写等非按键换行仍吞文本的问题（v1.5.0）**：这类换行不走键盘事件、直接产生 beforeinput，v1.5.0 里移动端兜底的 beforeinput 拦截会把它止住传播，而编辑器恰恰靠监听 beforeinput 同步内部状态——收不到就与 DOM 失同步，React 重新协调时把整行文本吞掉。根修：彻底删除 beforeinput 拦截，本脚本唯一的干预点收敛为「干净回车」的 keydown，其余一切输入事件（含输入法的所有文本/换行操作）全部放行，交给编辑器自己的管线

## v1.5.0

- **修复换行后整行文本消失的问题（v1.4.9）**：上一版用 execCommand 手动插入换行，与 Lexical/React 的状态同步冲突，换行时当前行文本会被吞掉（包括输入法整理文本触发的换行）。新方案彻底不碰 execCommand、不 preventDefault、不手动插内容：回车键在输入框里的原生默认行为本来就是换行，页面之所以发送是它自己把默认行为拦了——我们只在 window 捕获阶段止住事件传播（页面收不到、拦不了默认行为），浏览器原生的换行自然发生，编辑器状态零风险

## v1.4.9

- **修复回车换行拦截不住的问题（v1.4.8 及更早）**：此前拦截挂在 document 捕获阶段，而页面若把自己的回车监听挂在 window 捕获阶段，会在我们之前执行、直接把消息发出去。现改挂 window 捕获（注入早于页面脚本，先注册先执行）并用 stopImmediatePropagation 压住后注册的同层监听
- **Android/iOS 软键盘回车也改为换行**：软键盘回车常不产生可拦截的 keydown，补了 beforeinput 层的 insertParagraph 拦截。安全兜底：只有页面上找得到发送按钮时才拦截换行，找不到则保持原生「回车即发送」，保证消息永远发得出去（桌面端不受此限，可用 Cmd/Ctrl+Enter 发送）

